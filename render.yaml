services:
  - type: web
    name: nexus-business-suite
    env: node
    plan: free
    buildCommand: npm run build && npm rebuild
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: GEMINI_API_KEY
        sync: false
      - key: PORT
        value: 5000
